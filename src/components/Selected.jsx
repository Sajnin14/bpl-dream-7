import PropTypes from 'prop-types'

const Selected = () => {
    return (
        <div>
            <h2>Selected.jsx</h2>

        </div>
    );
};

Selected.propTypes = {
    selectedHandleChoosePlayer: PropTypes.func
}

export default Selected;